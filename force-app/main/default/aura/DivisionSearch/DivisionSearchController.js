({
    
    Search: function(cmp, event, helper) {

            helper.SearchHelper(cmp, event, helper); 

    },

    SearchAll : function(cmp, event, helper){

        helper.SearchAllHelper(cmp, event, helper);

    },

    Clear: function(cmp, event, helper) {

        helper.ClearHelper(cmp, event, helper);

    },

    EnableButton : function (cmp, event, helper){

        helper.EnableButtonHelper(cmp, event, helper);

    },

    TypingValidating : function (cmp, event, helper){

        helper.TypingValidatingHelper(cmp, event, helper);

    },

})